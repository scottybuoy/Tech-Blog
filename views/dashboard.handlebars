
<div>
    <h2>Posts</h2>
    {{#each posts as |post|}}
    <a href="/dashboard/post/{{post.id}}">
    <div>
            <h4>{{post.title}}</h4>
            <h6>{{format_date date_created}}</h6>
    </div>
    </a>
    {{/each}}
</div>



<button id="new-post-btn" type="submit" class="btn"><a href="/dashboard/new">New Post</a></button>